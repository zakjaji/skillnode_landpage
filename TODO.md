# Dropdown Design Update Plan

## Tasks
- [x] Replace first `<select>` (userTypeSelect) with custom dropdown HTML structure
- [x] Replace second `<select>` (experienceSelect) with custom dropdown HTML structure
- [x] Update CSS for `.custom-menu` (white background, 12px border-radius, shadow, spacing)
- [x] Update CSS for `.dropdown-item` (padding, font-size, colors, hover, active state)
- [x] Add CSS for `.checkmark` (white color, 12-14px, vertically centered)
- [x] Add JavaScript for custom dropdown interactions (toggle menu, select item, close on outside click)
- [x] Test dropdown functionality and ensure no breaking changes
